vi .zshrc
rm .zshrc
ls
vi .zcompdump
rm .zcompdump
rm .zshrc.zni
exit
ls
rg -h
vi .config
vi .rgrc
vi .config/zsh/.aliases.zsh
setopt
unsetopt extendedglob
unsetopt extendedglob || echo fail
unsetopt extendedglob && echo fail
unsetopt extendedglob && echo success
setopt extendedglob && echo success
vi $ZDOTDIR/.zshrc
zrc
zali
zrc
zfun
for f in ~/{?(.)*conf*?(ig),?(.)*rc,?(.)dircolors,.config/*/?(*conf*|*init*|*rc)}; do echo $f\

for f in ~/{?(.)*conf*?(ig),?(.)*rc,?(.)dircolors,.config/*/?(*conf*|*init*|*rc)}; do echo $f ; done\

setopt EXTENDED_GLOB NULL_GLOB\
for f in ~/{?(.)*conf?(ig),?(.)*rc,?(.)dircolors,.config/*/?(*conf*|*init*|*rc)}; do\
  echo ${f##~/}\
done
for f in ~/{?(.)*conf?\(ig\),?(.)*rc,?(.)dircolors,.config/*/?(*conf*|*init*|*rc)}; do\

for f in ~/{?(.)*conf*,?(.)*rc,?(.)dircolors,.config/*/?(*conf*|*init*|*rc)}; do
setopt EXTENDED_GLOB NULL_GLOB\
for f in ~/{?(.)*conf*,?(.)*rc,?(.)dircolors,.config/*/?(*conf*|*init*|*rc)}; do\
  echo ${f##~/}\
done
setopt EXTENDED_GLOB NULL_GLOB\
for f in ~/{?(.)*conf*,.*rc,?(.)dircolors,.config/*/?(*conf*|*init*|*rc)}; do\
  echo ${f##~/}\
done
ls
setopt EXTENDED_GLOB NULL_GLOB\
for f in ~/{.*conf*,.*rc,?(.)dircolors,.config/*/?(*conf*|*init*|*rc)}; do\
  echo ${f##~/}\
done
ls .config/nvim
setopt EXTENDED_GLOB NULL_GLOB\
for f in ~/{.*conf*,.*rc,?(.)dircolors,.config/*/?(*conf*|init*|*rc)}; do\
  echo ${f##~/}\
done
echo ~/.config/nvim/?(init*)
setopt EXTENDED_GLOB NULL_GLOB\
for f in ~/{.*conf*,.*rc,?(.)dircolors,.config/*/?(*conf*|*init*|*rc)}; do\
  echo ${f##~/}\
done
setopt EXTENDED_GLOB NULL_GLOB\
for f in ~/{.*conf*,.*rc,?(.)dircolors,.config/*/*{conf*,init*,*rc}}; do\
  echo ${f##~/}\
done
setopt EXTENDED_GLOB NULL_GLOB\
for f in ~/{.*conf,.*rc,?(.)dircolors,.config/*/*{conf*,init*,*rc}}; do\
  echo ${f##~/}\
done
setopt EXTENDED_GLOB NULL_GLOB\
for f in ~/{.*conf*,.*rc,?(.)dircolors,.config/*/*{conf*,init*,*rc}}; do\
  echo ${f##~/}\
done
exit
time (setopt EXTENDED_GLOB NULL_GLOB\
for f in ~/{.*conf*,.*rc,?(.)dircolors,.config/*/*{conf*,init*,*rc}}; do\
  echo ${f##~/}\
done)
vi /tmp/1
cmm /tmp/{1,2} -23
comm /tmp/{1,2} -23
comm /tmp/{1,2} -2
comm /tmp/{1,2}
comm -h
comm /tmp/{1,2} -12
comm /tmp/{1,2} -1 -2
comm /tmp/{1,2} -2
comm /tmp/{1,2} -3
setopt EXTENDED_GLOB NULL_GLOB\
for f in ~/{.*conf*,.*rc*,?(.)dircolors,.config/*/*{conf*,init*,*rc}}; do\
  echo ${f##~/}\
done
vi /tmp/2
comm /tmp/{1,2} -3
bali
zali
zfun
zrc
fdrc
zfun
fdrc
fdrc | sort
zfun
vi .bashrc
zenv
zali
zrc
zenv
zfun
fdrc
P=$(fdrc)
P=$(fdrc) echo $P
P=$(fdrc) echo ${P#.config}
P=(){fdrc} echo ${P#.config}
zrc
fdrc
zrc
fdrc
comm -3 /tmp/{1,2}
fdrc
zrc
fdrc
zrc
fdrc
zrc
fdrc
zrc
fdrc
ls .config
zrc
fdrc
cd dotfiles
edrc
a
zrc
edrc
zrc
edrc
ls
erdcd
ercgd
ecrd
zrc
edcrd
ercdh
ergcd
ercd
edrc
edcr
zrc
edcr
edcredrc
derc
zrc
derc
zrc
derc
decr
cd
exit
zali
edrc
zali
zfun
zrc
edrc
fdrc
zali
zfun
ls $ZDOTDIR
ls .config/zsh
ls -la .config/zsh
zali
zfun
zrc
fdrc
edrc
  zrc
cp IMG20260423183738.jpg
zrc
setopt AUTO_LIST
zc
zrc
rd
for f in ~/{.*{.,git}conf*,.*rc*,.config/*/{*conf*,*init*,.*zsh*}}; do echo ${f##~/}; done
zrc
ls -la
zrc
f
fdrc 
zrc
push yocto/yocto
push yocto/
pushd yocto/
ls
cd
ls
cd -1
ls
cd -2
cd -1
pushd yocto
pushd ~/suckless
cd -1
ls
cd -0
ls
zrc
cd suckless
cd ..
cd suckless/st/
vi config.h
cd
cd ble-0.4.0-devel3
cd ..
zrc
echo $EDITOR
zrc
echo ${EDITOR##/*/}
man man zshcompwid
man zshcompwid
zrc
grep
gerp
grep
zrc
gerp
alias gerp=grep
gerp
rg
gepr
zrc
gerp
gepr
grep
edrc
zrc
popd.
popd .
vi .inputrc
ls
a
zrc
asontehuethuc
ls
exit
